# CoLA dependencies. Install with: pip install -r requirements.txt
#
# jaxlib 0.4.20 needs the numpy 1.x ABI, so install numpy==1.24.3 last
# (otherwise: "AttributeError: _ARRAY_API not found").

# Core ML frameworks
tensorflow==2.15.0
tensorflow-probability==0.23.0

# JAX ecosystem (Octo)
jax==0.4.20
jaxlib==0.4.20
flax==0.7.5
optax==0.1.5
chex==0.1.85
orbax-checkpoint==0.4.4

# HuggingFace
transformers==4.34.1
tokenizers==0.14.1
huggingface-hub==0.17.3
safetensors==0.8.0
# accelerate is omitted: it needs huggingface-hub>=0.21, which tokenizers 0.14.1 excludes.

# Data handling
h5py==3.14.0
scipy==1.12.0
# Linear probes on the messages (analysis/)
scikit-learn

# Visualization
matplotlib==3.10.9
pillow==12.3.0
# Video writing (mediapy needs ffmpeg; imageio-ffmpeg bundles one)
mediapy
imageio
imageio-ffmpeg

# Utilities
einops==0.8.2
tqdm==4.68.4
PyYAML==6.0.3
click==8.4.2
rich==15.0.0

# ML utilities
ml_collections==1.1.0
dm-tree==0.1.10
distrax==0.1.5
tensorstore==0.1.45

# TensorBoard
tensorboard==2.15.2

# Experiment tracking
wandb

# Networking
requests==2.34.2
httpx==0.28.1
fsspec==2026.6.0

# Other
absl-py==2.5.0
cloudpickle==3.1.2
nest-asyncio==1.6.0
packaging==26.0
psutil==7.2.2
regex==2026.7.10
sympy==1.14.0
typer==0.27.0

# MuJoCo (simulation and data collection), the version used for all results
mujoco==3.12.0
# IK for the scripted expert (mink, with the daqp QP solver)
mink
loop_rate_limiters
qpsolvers[daqp]

# Install last (see the numpy note above)
numpy==1.24.3
